<h2>Games</h2>

<ul>
  <% games.forEach(function(game) { %>
    <li>
      <a href="<%= "/game/" + game.gameName %>">
        <%= game.gameName %>
      </a>

      ----------------

      <a class="delete-link" href="<%= "/game/" + game.gameName %>">
        Delete
      </a>
    </li>
  <% }) %>
</ul>
